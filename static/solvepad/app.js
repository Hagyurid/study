/* SolvePad MultiFile Local v5.4
   Extracted from the stable v5.3 one-file build.
   Data is stored locally in the browser IndexedDB. */

'use strict';
const DB_NAME='solvepad_v5_local',DB_VERSION=1,MAX_ZOOM=3,MIN_ZOOM=.6;let db;
const $=id=>document.getElementById(id);const $$=sel=>Array.from(document.querySelectorAll(sel));
const el={app:$('app'),sidebarToggle:$('sidebarToggle'),packTitle:$('packTitle'),packMeta:$('packMeta'),progressFill:$('progressFill'),progressText:$('progressText'),mainTitle:$('mainTitle'),mainSub:$('mainSub'),questionList:$('questionList'),qBadge:$('qBadge'),qTitle:$('qTitle'),qTags:$('qTags'),questionBody:$('questionBody'),choices:$('choices'),assetArea:$('assetArea'),penBtn:$('penBtn'),eraserBtn:$('eraserBtn'),penWidth:$('penWidth'),undoBtn:$('undoBtn'),clearBtn:$('clearBtn'),saveImageBtn:$('saveImageBtn'),paperViewport:$('paperViewport'),paper:$('paper'),canvas:$('ink'),pageInfo:$('pageInfo'),zoomInfo:$('zoomInfo'),penOnly:$('penOnly'),drawer:$('sideDrawer'),drawerTitle:$('drawerTitle'),drawerBody:$('drawerBody'),importModal:$('importModal'),libraryModal:$('libraryModal'),jsonInput:$('jsonInput'),jsonFile:$('jsonFile'),serverUrl:$('serverUrl'),subjectFilter:$('subjectFilter'),folderFilter:$('folderFilter'),packList:$('packList'),wrongList:$('wrongList'),storageInfo:$('storageInfo'),backupFile:$('backupFile')};
const samplePack={schemaVersion:'solvepad.problemPack.v5',packId:'sample-math2-limit-v53',title:'수학 II 극한 예시 문제팩',description:'SolvePad v5.3 테스트용 샘플',subject:'수학 II',folder:'함수의 극한',unit:'함수의 극한',author:'SolvePad',createdAt:'2026-06-07',settings:{defaultCanvasHeight:900,answerRevealMode:'manual'},questions:[{id:'q001',order:1,section:'극한',title:'문제 1',type:'short_answer',difficulty:2,estimatedMinutes:3,promptMd:'다음 극한값을 구하시오.\n\n$$\\lim_{x\\to 2}\\frac{x^2-4}{x-2}$$',choices:null,assets:[],answer:{type:'text',value:'4',acceptable:['4']},solution:{concepts:['분모와 분자가 동시에 0이 되는 꼴에서는 인수분해를 먼저 확인한다.'],actualSolution:['$x^2-4=(x-2)(x+2)$ 이다.','$x\\ne2$에서 식은 $x+2$로 정리된다.','따라서 극한값은 $4$이다.'],cautions:['$x=2$를 원식에 바로 대입하면 분모가 0이 된다.'],tips:['$0/0$ 꼴은 인수분해, 유리화, 약분을 먼저 본다.']},hints:['분자를 인수분해해 보자.'],canvas:{height:900,template:'grid'},tags:['극한','인수분해']},{id:'q002',order:2,section:'이미지형',title:'문제 2',type:'essay',difficulty:3,estimatedMinutes:5,promptMd:'아래 그래프 이미지를 보고 $x\\to 1$일 때의 좌극한과 우극한을 비교하시오.',choices:null,assets:[{id:'q002_graph',type:'image',required:true,label:'문제 2 그래프 이미지',description:'그래프 사진을 직접 업로드하세요.'}],answer:{type:'text',value:'이미지에 따라 판단',acceptable:['이미지 확인 필요']},solution:{concepts:['좌극한과 우극한은 각각 왼쪽과 오른쪽에서 접근할 때의 함수값 경향이다.'],actualSolution:['업로드된 그래프를 기준으로 왼쪽 접근값과 오른쪽 접근값을 각각 읽는다.','두 값이 같으면 극한이 존재하고, 다르면 존재하지 않는다.'],cautions:['그래프의 열린 점과 닫힌 점을 함수값과 극한값으로 혼동하지 않는다.'],tips:['기준점 양옆을 따로 가리고 보면 빠르다.']},hints:['그래프에서 x=1의 왼쪽과 오른쪽을 따로 보자.'],canvas:{height:980,template:'grid'},tags:['그래프','좌우극한']} ]};
const state={pack:null,idx:0,filter:'all',tool:'pen',pages:[[]],pageIdx:0,zoom:1,penOnly:true,drawing:false,currentStroke:null,activePointerId:null,raf:0,dirty:false};
const pinch={pointers:new Map(),active:false,startDistance:0,startZoom:1};const ctx=el.canvas.getContext('2d');let dpr=1,saveTimer=0;
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function md(s){return esc(s).replace(/\n/g,'<br>').replace(/\*\*(.*?)\*\*/g,'<b>$1</b>')}
function q(){return state.pack?.questions?.[state.idx]||null}function key(qid){return `${state.pack.packId}::${qid}`}function assetKey(qid,aid){return `${state.pack.packId}::${qid}::${aid}`}
function openDB(){return new Promise((res,rej)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=e=>{const d=e.target.result;for(const s of ['packs','progress','strokes','assets'])if(!d.objectStoreNames.contains(s))d.createObjectStore(s,{keyPath:'id'})};r.onsuccess=()=>{db=r.result;res(db)};r.onerror=()=>rej(r.error)})}
function store(name,mode='readonly'){return db.transaction(name,mode).objectStore(name)}function req(r){return new Promise((res,rej)=>{r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)})}const put=(s,v)=>req(store(s,'readwrite').put(v));const get=(s,id)=>req(store(s).get(id));const del=(s,id)=>req(store(s,'readwrite').delete(id));const all=s=>req(store(s).getAll());
function validatePack(p){if(!p||!p.packId||!p.title||!Array.isArray(p.questions))throw new Error('packId/title/questions가 필요합니다.');const ids=new Set();p.questions.forEach((x,i)=>{if(!x.id)throw new Error(`${i+1}번 문제 id 누락`);if(ids.has(x.id))throw new Error(`${x.id} 문제 id 중복`);ids.add(x.id);if(!x.solution?.concepts||!x.solution?.actualSolution||!x.solution?.cautions||!x.solution?.tips)throw new Error(`${x.id} 해설 4항목 누락`)})}
async function savePack(p){validatePack(p);p.savedAt=new Date().toISOString();await put('packs',{id:p.packId,pack:p});state.pack=p;state.idx=0;await loadStrokes();await renderAll();await renderLibrary()}
async function loadPack(id){const row=await get('packs',id);if(!row)return alert('문제팩 없음');state.pack=row.pack;state.idx=0;await loadStrokes();await renderAll();closeModals()}
async function renderAll(){renderPackInfo();await renderQList();await renderQuestion();await renderProgress();resizeCanvas()}
function renderPackInfo(){const p=state.pack;el.packTitle.textContent=p?p.title:'없음';el.packMeta.textContent=p?`${p.subject||'-'} · ${p.folder||p.unit||'-'} · ${p.questions.length}문항`:'문제팩을 불러오세요.';el.mainTitle.textContent=p?p.title:'문제팩을 불러오세요';el.mainSub.textContent=p?p.description||'':'GPT JSON을 붙여넣거나 import 링크를 여세요.'}
async function renderProgress(){if(!state.pack){el.progressFill.style.width='0%';el.progressText.textContent='0 / 0';return}let done=0;for(const qu of state.pack.questions){const pr=await get('progress',key(qu.id));if(pr?.status)done++}const pct=Math.round(done/state.pack.questions.length*100);el.progressFill.style.width=pct+'%';el.progressText.textContent=`${done} / ${state.pack.questions.length} · ${pct}%`}
async function renderQList(){el.questionList.innerHTML='';if(!state.pack)return;for(let i=0;i<state.pack.questions.length;i++){const qu=state.pack.questions[i],pr=await get('progress',key(qu.id));if(state.filter==='wrong'&&pr?.status!=='wrong')continue;if(state.filter==='book'&&!pr?.bookmarked)continue;const b=document.createElement('button');b.className='qitem'+(i===state.idx?' active':'');b.innerHTML=`<span>${qu.order||i+1}. ${esc(qu.title||qu.id)}</span><span>${pr?.status==='wrong'?'<span class="badge wrong">오답</span>':''}${pr?.status==='correct'?'<span class="badge ok">맞음</span>':''}${pr?.bookmarked?'<span class="badge book">★</span>':''}</span>`;b.onclick=async()=>{await saveStrokesNow();state.idx=i;await loadStrokes();await renderAll()};el.questionList.appendChild(b)}}
async function renderQuestion(){const qu=q();el.choices.innerHTML='';el.assetArea.innerHTML='';if(!qu){el.qTitle.textContent='-';el.questionBody.textContent='문제 없음';renderDrawer('answer');return}el.qBadge.textContent=`문제 ${qu.order||state.idx+1}`;el.qTitle.textContent=qu.title||qu.id;el.qTags.innerHTML=(qu.tags||[]).map(t=>`<span class="badge">${esc(t)}</span>`).join('');el.questionBody.innerHTML=md(qu.promptMd||'');if(Array.isArray(qu.choices)){for(const c of qu.choices){const d=document.createElement('div');d.className='choice';d.innerHTML=`<b>${esc(c.id)}.</b> ${md(c.text)}`;el.choices.appendChild(d)}}await renderAssets(qu);renderDrawer('answer');const h=qu.canvas?.height||state.pack.settings?.defaultCanvasHeight||900;const vh=Math.max(560,Math.min(h,window.innerHeight-260));el.paperViewport.style.height=vh+'px';el.paper.style.height=vh+'px';state.pageIdx=0;renderPageBar()}
async function renderAssets(qu){for(const a of qu.assets||[]){const div=document.createElement('div');div.className='asset-box';div.innerHTML=`<b>${esc(a.label||a.id)}</b><div class="sub">${esc(a.description||'이미지가 필요한 문제입니다.')}</div>`;if(a.type==='svg'&&a.content){div.insertAdjacentHTML('beforeend',a.content)}else{const saved=await get('assets',assetKey(qu.id,a.id));if(saved?.dataUrl)div.insertAdjacentHTML('beforeend',`<img src="${saved.dataUrl}" alt="${esc(a.label||a.id)}">`);const inp=document.createElement('input');inp.type='file';inp.accept='image/*';inp.onchange=async()=>{const f=inp.files?.[0];if(!f)return;await put('assets',{id:assetKey(qu.id,a.id),packId:state.pack.packId,questionId:qu.id,assetId:a.id,dataUrl:await fileToDataUrl(f),createdAt:new Date().toISOString()});await renderQuestion()};div.appendChild(inp)}el.assetArea.appendChild(div)}}
function fileToDataUrl(f){return new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=()=>rej(r.error);r.readAsDataURL(f)})}
function answerHTML(qu){if(!qu)return '문제를 불러오세요.';const a=qu.answer||{},s=qu.solution||{};return `<div class="okbox"><b>정답:</b> ${esc(a.value||a.choiceIds?.join(', ')||'-')}</div><h3>1. 문제와 관련된 개념</h3><ol>${(s.concepts||[]).map(x=>`<li>${md(x)}</li>`).join('')}</ol><h3>2. 문제 실제 풀이</h3><ol>${(s.actualSolution||[]).map(x=>`<li>${md(x)}</li>`).join('')}</ol><h3>3. 문제 풀이시 주의사항</h3><ol>${(s.cautions||[]).map(x=>`<li>${md(x)}</li>`).join('')}</ol><h3>4. 문제 풀이 꿀팁</h3><ol>${(s.tips||[]).map(x=>`<li>${md(x)}</li>`).join('')}</ol>`}
function hintHTML(qu){return qu&&(qu.hints||[]).length?`<ol>${qu.hints.map(x=>`<li>${md(x)}</li>`).join('')}</ol>`:'힌트 없음'}
function renderDrawer(kind='answer'){const qu=q();el.drawerTitle.textContent=kind==='hint'?'힌트':'답지/해설';el.drawerBody.className=kind==='hint'?'':'solution';el.drawerBody.innerHTML=kind==='hint'?hintHTML(qu):answerHTML(qu)}
function pageStrokes(){if(!state.pages.length)state.pages=[[]];if(!state.pages[state.pageIdx])state.pages[state.pageIdx]=[];return state.pages[state.pageIdx]}
function renderPageBar(){const total=Math.max(1,state.pages.length);state.pageIdx=Math.min(Math.max(0,state.pageIdx),total-1);el.pageInfo.textContent=`${state.pageIdx+1} / ${total}`;el.zoomInfo.textContent=Math.round(state.zoom*100)+'%';el.paper.style.setProperty('--grid',(24*state.zoom)+'px')}
function resizeCanvas(){const r=el.paper.getBoundingClientRect();dpr=window.devicePixelRatio||1;el.canvas.width=Math.max(1,Math.floor(r.width*dpr));el.canvas.height=Math.max(1,Math.floor(r.height*dpr));el.canvas.style.width=r.width+'px';el.canvas.style.height=r.height+'px';drawStrokes()}
function scheduleDraw(){if(state.raf)return;state.raf=requestAnimationFrame(()=>{state.raf=0;drawStrokes();if(state.currentStroke)drawStroke(state.currentStroke)})}
function drawStroke(s){if(!s.points||s.points.length<2)return;ctx.strokeStyle=s.color||'#111';ctx.lineWidth=s.width||4;ctx.beginPath();ctx.moveTo(s.points[0].x,s.points[0].y);for(let i=1;i<s.points.length-1;i++){const p=s.points[i],n=s.points[i+1];ctx.quadraticCurveTo(p.x,p.y,(p.x+n.x)/2,(p.y+n.y)/2)}ctx.lineTo(s.points[s.points.length-1].x,s.points[s.points.length-1].y);ctx.stroke()}
function drawStrokes(){ctx.setTransform(1,0,0,1,0,0);ctx.clearRect(0,0,el.canvas.width,el.canvas.height);ctx.setTransform(dpr*state.zoom,0,0,dpr*state.zoom,0,0);ctx.lineCap='round';ctx.lineJoin='round';for(const s of pageStrokes())drawStroke(s)}
function pointerAllowed(e){return !state.penOnly||e.pointerType==='pen'}function eventPoints(e){return e.getCoalescedEvents?e.getCoalescedEvents():[e]}function pos(e){const r=el.canvas.getBoundingClientRect();return{x:(e.clientX-r.left)/state.zoom,y:(e.clientY-r.top)/state.zoom}}
function distPointToSegment(p,a,b){const vx=b.x-a.x,vy=b.y-a.y,wx=p.x-a.x,wy=p.y-a.y,c1=vx*wx+vy*wy;if(c1<=0)return Math.hypot(p.x-a.x,p.y-a.y);const c2=vx*vx+vy*vy;if(c2<=c1)return Math.hypot(p.x-b.x,p.y-b.y);const t=c1/c2;return Math.hypot(p.x-(a.x+t*vx),p.y-(a.y+t*vy))}
function distToStroke(pt,s){const ps=s.points||[];if(ps.length<2)return 1e9;let m=1e9;for(let i=0;i<ps.length-1;i++)m=Math.min(m,distPointToSegment(pt,ps[i],ps[i+1]));return m}
function eraseAt(pt){const rad=((+el.penWidth.value||4)*3+14)/state.zoom,arr=pageStrokes(),before=arr.length;state.pages[state.pageIdx]=arr.filter(s=>distToStroke(pt,s)>rad);if(before!==state.pages[state.pageIdx].length){drawStrokes();queueSave()}}
function finishStroke(){if(!state.drawing)return;if(state.currentStroke&&state.currentStroke.points.length>1)pageStrokes().push(state.currentStroke);state.currentStroke=null;state.drawing=false;state.activePointerId=null;queueSave();drawStrokes()}
function updateTouch(e){if(e.pointerType!=='touch')return false;pinch.pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});return true}function removeTouch(e){if(e.pointerType!=='touch')return false;pinch.pointers.delete(e.pointerId);if(pinch.pointers.size<2){pinch.active=false;pinch.startDistance=0}return true}function pinchDistance(){const a=[...pinch.pointers.values()];return a.length<2?0:Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y)}function startPinch(){if(pinch.pointers.size<2)return false;finishStroke();pinch.active=true;pinch.startDistance=pinchDistance()||1;pinch.startZoom=state.zoom;return true}function handlePinch(e){if(e.pointerType!=='touch')return false;updateTouch(e);if(!pinch.active&&pinch.pointers.size>=2)startPinch();if(!pinch.active)return false;setZoom(pinch.startZoom*(pinchDistance()/pinch.startDistance));return true}
el.canvas.addEventListener('pointerdown',e=>{if(!q())return;if(e.pointerType==='touch'){try{el.canvas.setPointerCapture(e.pointerId)}catch{}updateTouch(e);if(pinch.pointers.size>=2){e.preventDefault();startPinch();return}if(state.penOnly){e.preventDefault();return}}if(!pointerAllowed(e))return;e.preventDefault();try{el.canvas.setPointerCapture(e.pointerId)}catch{}state.activePointerId=e.pointerId;const p=pos(e);if(state.tool==='eraser'){eraseAt(p);return}const base=+el.penWidth.value||4,pressure=e.pressure&&e.pressure>0?(.75+e.pressure*.55):1;state.drawing=true;state.currentStroke={id:crypto.randomUUID?.()||String(Date.now()),color:'#111',width:base*pressure,points:[p]}});
el.canvas.addEventListener('pointermove',e=>{if(!q())return;if(e.pointerType==='touch'){if(handlePinch(e)||state.penOnly){e.preventDefault();return}}if(state.activePointerId!==null&&e.pointerId!==state.activePointerId)return;if(!pointerAllowed(e))return;e.preventDefault();for(const ev of eventPoints(e)){const p=pos(ev);if(state.tool==='eraser'&&e.buttons){eraseAt(p);continue}if(state.drawing)state.currentStroke.points.push(p)}if(state.drawing)scheduleDraw()});
for(const type of ['pointerup','pointercancel','lostpointercapture'])el.canvas.addEventListener(type,e=>{if(e.pointerType==='touch'){removeTouch(e);return}if(state.activePointerId===null||state.activePointerId===e.pointerId||type==='lostpointercapture')finishStroke()});
function queueSave(){clearTimeout(saveTimer);saveTimer=setTimeout(saveStrokesNow,250)}async function saveStrokesNow(){const qu=q();if(!state.pack||!qu)return;clearTimeout(saveTimer);await put('strokes',{id:key(qu.id),packId:state.pack.packId,questionId:qu.id,pages:state.pages,updatedAt:new Date().toISOString()})}
async function loadStrokes(){const qu=q();state.pages=[[]];state.pageIdx=0;if(!state.pack||!qu){renderPageBar();drawStrokes();return}const row=await get('strokes',key(qu.id));state.pages=row?.pages||[row?.strokes||[]];if(!Array.isArray(state.pages)||!state.pages.length)state.pages=[[]];renderPageBar();drawStrokes()}
async function setProgress(patch){const qu=q();if(!qu)return;const id=key(qu.id),cur=await get('progress',id)||{id,packId:state.pack.packId,questionId:qu.id};await put('progress',{...cur,...patch,updatedAt:new Date().toISOString()});await renderQList();await renderProgress()}
async function toggleBookmark(){const qu=q();if(!qu)return;const cur=await get('progress',key(qu.id));await setProgress({bookmarked:!cur?.bookmarked})}
function setZoom(z){state.zoom=Math.max(MIN_ZOOM,Math.min(MAX_ZOOM,z||1));renderPageBar();drawStrokes()}function prevPage(){state.pageIdx=Math.max(0,state.pageIdx-1);renderPageBar();drawStrokes()}function nextPage(){if(state.pageIdx>=state.pages.length-1)state.pages.push([]);state.pageIdx++;renderPageBar();drawStrokes();queueSave()}function addPage(){state.pages.push([]);state.pageIdx=state.pages.length-1;renderPageBar();drawStrokes();queueSave()}
async function renderLibrary(){const packs=await all('packs'),progresses=await all('progress');const subjects=['전체',...new Set(packs.map(x=>x.pack.subject||'미분류'))],folders=['전체',...new Set(packs.map(x=>x.pack.folder||x.pack.unit||'미분류'))];el.subjectFilter.innerHTML=subjects.map(x=>`<option>${esc(x)}</option>`).join('');el.folderFilter.innerHTML=folders.map(x=>`<option>${esc(x)}</option>`).join('');await renderPackList();await renderWrongList(progresses);if(navigator.storage?.estimate){const e=await navigator.storage.estimate();el.storageInfo.textContent=`예상 사용량 ${Math.round((e.usage||0)/1048576)}MB / ${Math.round((e.quota||0)/1048576)}MB`}}
async function renderPackList(){const packs=await all('packs'),sf=el.subjectFilter.value||'전체',ff=el.folderFilter.value||'전체';el.packList.innerHTML='';for(const row of packs){const p=row.pack;if(sf!=='전체'&&(p.subject||'미분류')!==sf)continue;if(ff!=='전체'&&(p.folder||p.unit||'미분류')!==ff)continue;const d=document.createElement('div');d.className='pack-card';d.innerHTML=`<b>${esc(p.title)}</b><div class="sub">${esc(p.subject||'-')} · ${esc(p.folder||p.unit||'-')} · ${p.questions.length}문항</div><div class="row" style="margin-top:8px"><button class="small open">열기</button><button class="small danger delete">삭제</button></div>`;d.querySelector('.open').onclick=()=>loadPack(p.packId);d.querySelector('.delete').onclick=async()=>{if(confirm('삭제할까요?')){await del('packs',p.packId);if(state.pack?.packId===p.packId){state.pack=null;state.idx=0;await renderAll()}await renderLibrary()}};el.packList.appendChild(d)}}
async function renderWrongList(progresses){const packs=await all('packs'),map=new Map(packs.map(x=>[x.pack.packId,x.pack])),wrong=progresses.filter(x=>x.status==='wrong');el.wrongList.innerHTML=wrong.length?'':'<div class="sub">오답 없음</div>';for(const w of wrong){const p=map.get(w.packId),qu=p?.questions.find(x=>x.id===w.questionId);if(!p||!qu)continue;const b=document.createElement('button');b.textContent=`${p.subject||'-'} / ${p.folder||p.unit||'-'} / ${p.title} / ${qu.title||qu.id}`;b.onclick=async()=>{state.pack=p;state.idx=p.questions.findIndex(x=>x.id===qu.id);await loadStrokes();await renderAll();closeModals()};el.wrongList.appendChild(b)}}
function openModal(m){m.classList.add('show')}function closeModals(){$$('.modal').forEach(m=>m.classList.remove('show'))}
async function handleImportToken(){const url=new URL(location.href),token=url.searchParams.get('importToken')||url.searchParams.get('token');if(!token)return;const server=url.searchParams.get('server')||localStorage.getItem('solvepad_action_server_url');if(!server){alert('Action 서버 URL이 필요합니다. 문제 불러오기에서 서버 URL을 저장하세요.');return}const res=await fetch(`${server.replace(/\/$/,'')}/packs/${encodeURIComponent(token)}`);if(!res.ok)throw new Error('서버에서 문제팩을 가져오지 못했습니다.');const data=await res.json();await savePack(data.pack||data);history.replaceState(null,'',location.pathname)}
function download(name,content,type='text/plain'){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([content],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}async function exportBackup(){download('solvepad-backup.json',JSON.stringify({version:'solvepad-backup-v5.4',createdAt:new Date().toISOString(),packs:await all('packs'),progress:await all('progress'),strokes:await all('strokes'),assets:await all('assets')},null,2),'application/json')}async function importBackupFile(f){const b=JSON.parse(await f.text());for(const s of ['packs','progress','strokes','assets'])for(const row of b[s]||[])await put(s,row);await renderLibrary();alert('백업 가져오기 완료')}
function saveCurrentPageImage(){const r=el.paper.getBoundingClientRect(),tmp=document.createElement('canvas');tmp.width=Math.round(r.width);tmp.height=Math.round(r.height);const c=tmp.getContext('2d');c.fillStyle='#fffdf7';c.fillRect(0,0,tmp.width,tmp.height);c.strokeStyle='#e7dfd1';for(let x=0;x<tmp.width;x+=24*state.zoom){c.beginPath();c.moveTo(x,0);c.lineTo(x,tmp.height);c.stroke()}for(let y=0;y<tmp.height;y+=24*state.zoom){c.beginPath();c.moveTo(0,y);c.lineTo(tmp.width,y);c.stroke()}c.drawImage(el.canvas,0,0,tmp.width,tmp.height);tmp.toBlob(b=>{const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='solvepad-writing.png';a.click()},'image/png')}
function bindEvents(){el.sidebarToggle.onclick=()=>el.app.classList.toggle('collapsed');$('openImport').onclick=()=>openModal(el.importModal);$('quickImport').onclick=()=>openModal(el.importModal);$('openLibrary').onclick=async()=>{await renderLibrary();openModal(el.libraryModal)};$('quickLibrary').onclick=async()=>{await renderLibrary();openModal(el.libraryModal)};$$('.closeModal').forEach(b=>b.onclick=closeModals);$('addSample').onclick=()=>savePack(samplePack);$('applyJson').onclick=async()=>{try{await savePack(JSON.parse(el.jsonInput.value));closeModals()}catch(e){alert(e.message)}};el.jsonFile.onchange=async()=>{const f=el.jsonFile.files?.[0];if(f)el.jsonInput.value=await f.text()};el.serverUrl.onchange=()=>localStorage.setItem('solvepad_action_server_url',el.serverUrl.value.trim());$('prevQ').onclick=async()=>{if(!state.pack)return;await saveStrokesNow();state.idx=Math.max(0,state.idx-1);await loadStrokes();await renderAll()};$('nextQ').onclick=async()=>{if(!state.pack)return;await saveStrokesNow();state.idx=Math.min(state.pack.questions.length-1,state.idx+1);await loadStrokes();await renderAll()};el.penBtn.onclick=()=>{state.tool='pen';el.penBtn.classList.add('primary');el.eraserBtn.classList.remove('primary')};el.eraserBtn.onclick=()=>{state.tool='eraser';el.eraserBtn.classList.add('primary');el.penBtn.classList.remove('primary')};el.undoBtn.onclick=()=>{pageStrokes().pop();drawStrokes();queueSave()};el.clearBtn.onclick=()=>{if(confirm('현재 쪽 풀이를 지울까요?')){state.pages[state.pageIdx]=[];drawStrokes();queueSave()}};el.saveImageBtn.onclick=saveCurrentPageImage;$('railAnswer').onclick=()=>{renderDrawer('answer');el.drawer.classList.add('show')};$('railHint').onclick=()=>{renderDrawer('hint');el.drawer.classList.add('show')};$('closeDrawer').onclick=()=>el.drawer.classList.remove('show');$('railCorrect').onclick=()=>setProgress({status:'correct'});$('railWrong').onclick=()=>setProgress({status:'wrong'});$('railBook').onclick=toggleBookmark;$('railPrevPage').onclick=prevPage;$('railNextPage').onclick=nextPage;$('prevPage').onclick=prevPage;$('nextPage').onclick=nextPage;$('addPage').onclick=addPage;$('zoomIn').onclick=()=>setZoom(state.zoom+.15);$('zoomOut').onclick=()=>setZoom(state.zoom-.15);$('zoomReset').onclick=()=>setZoom(1);el.penOnly.onchange=()=>state.penOnly=el.penOnly.checked;$('filterAll').onclick=()=>{state.filter='all';renderQList()};$('filterWrong').onclick=()=>{state.filter='wrong';renderQList()};$('filterBook').onclick=()=>{state.filter='book';renderQList()};$('showWrongAll').onclick=async()=>{await renderLibrary();openModal(el.libraryModal)};el.subjectFilter.onchange=renderPackList;el.folderFilter.onchange=renderPackList;$('exportBackup').onclick=exportBackup;$('importBackup').onclick=()=>{const f=el.backupFile.files?.[0];if(f)importBackupFile(f)};window.addEventListener('resize',resizeCanvas)}
(async function init(){bindEvents();await openDB();el.serverUrl.value=localStorage.getItem('solvepad_action_server_url')||'';try{await handleImportToken()}catch(e){alert('importToken 오류: '+e.message)}state.penOnly=el.penOnly.checked;await renderAll();await renderLibrary();resizeCanvas()})();